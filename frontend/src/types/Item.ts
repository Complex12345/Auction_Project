export interface Item {
    id: number,
    name: string,
    description: string,
    category: string,
    imageURL: string,
    condition: string,
    auctionEndTime: string,
    currentBid: number
}